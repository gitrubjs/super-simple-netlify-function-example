import React from 'react';
import {render} from 'react-dom';

function App(){
    return(
        <div>
            Welcome to React.
        </div>
    )
}

render(<App/>, document.getElementById('*'));